```
$ sudo yum install zsh
$ sudo chsh ec2-user -s /bin/zsh
$ cd ~/dotfiles
$ git pull
$ git submodule init
$ git submodule update
$ init.sh
```

やること
bashならzsh切り替え
#zsh install
sudo yum install zsh
# zshに切り替え
sudo chsh {user-name} -s {zsh_path}
# chsh -s {zsh_path}
#   -> /etc/shells確認 おそらく /bin/zsh
